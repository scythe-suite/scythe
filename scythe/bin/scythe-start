echo2 "Activating tm"

ssh "$REMOTE_HOST" "$REMOTE_RUN_COMMAND"
sleep 1

echo2 "Checking version hash and logs"

tm hashconf "$TM_SETTINGS"
curl -s "$REMOTE_ENDPOINT" && echo
ssh "$REMOTE_HOST" "tail '$REMOTE_UPLOADS/EVENTS'*"
