echo1 "Inquiring tm status on remote server '$SCYTHE_SERVER'"

ssh "$SCYTHE_USER@$SCYTHE_SERVER" "status $SCYTHE_EXAM_ID"

echo1 "Checking local and remote version configuration hash"

tm hashconf "$TM_SETTINGS"
sleep 1
curl -s "$REMOTE_ENDPOINT" && echo
