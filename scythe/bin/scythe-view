#!/bin/bash

export SFLAB="$( cd "$( dirname "${BASH_SOURCE[0]}" )/.." && pwd )"
source "$SCYTHE_HOME/../confs/confs.sh" "$1"

echo1 "Opening the results viewer"

sv results.json:"$LOCAL_RESULTS/results.json"
