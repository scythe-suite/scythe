echo1 "Getting harvest from remote server '$SCYTHE_SERVER'"

mkdir -p "$HARVEST"
rsync -e "ssh $SCYTHE_USER@$SCYTHE_SERVER get $SCYTHE_EXAM_ID" -zPav $SCYTHE_USER@$SCYTHE_SERVER:NOT_RELEVANT "${HARVEST}"
